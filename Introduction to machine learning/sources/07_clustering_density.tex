\section*{CLUSTERING / ASSOCIATION}
\subsection*{K-MEANS}
\[
\fid[eq:kmeans]\min_{\{C_k,\mu_k\}}\sum_{k=1}^K\sum_{i\in C_k}\|x_i-\mu_k\|^2,\quad
\mu_k=\frac1{|C_k|}\sum_{i\in C_k}x_i
\]
MEANS: $C_k$ = rows assigned to cluster $k$; $\mu_k$ = centroid/mean vector; $|C_k|$ = cluster size. CALC: assign each row to nearest centroid, recompute centroid as coordinate-wise mean, repeat if asked. READ CENTROID TABLE: high standardized coordinate = cluster is above average on that attribute; cluster labels have no natural order.

\subsection*{HIERARCHICAL CLUSTERING}
\begin{tabularx}{\linewidth}{@{}lX@{}}
\toprule
LINKAGE & CLUSTER DISTANCE\\ \midrule
SINGLE / MINIMUM & smallest pair distance across two clusters; can chain.\\
COMPLETE & largest pair distance; compact clusters.\\
AVERAGE & average of all cross-cluster pair distances.\\
CENTROID & distance between cluster means.\\
\bottomrule
\end{tabularx}
READ DENDROGRAM: leaves = rows; merge height = distance where groups join; first merge has smallest distance. CALC: to get $K$ clusters, cut horizontally so $K$ branches remain.

\subsection*{RAND / JACCARD CLUSTER AGREEMENT}
\[
\fid[eq:cluster-agree]\operatorname{Rand}=\frac{f_{11}+f_{00}}{\binom N2},\quad
J=\frac{f_{11}}{f_{11}+f_{10}+f_{01}}
\]
MEANS: inspect row pairs; $f_{11}$ = together in both clusterings; $f_{00}$ = apart in both; $f_{10},f_{01}$ = together in one but apart in other. READ: Rand counts both same-cluster and different-cluster agreement; Jaccard ignores shared separation.

\subsection*{LOCAL DENSITY / OUTLIER}
\[
\fid[eq:local-density]\begin{aligned}
\operatorname{dens}(x_i,K)&=
\left(\frac1K\sum_{x_j\in N_i}d(x_i,x_j)\right)^{-1},\\
\operatorname{ARD}(x_i,K)&=\frac1K\sum_{x_j\in N_i}
\frac{\operatorname{dens}(x_i,K)}{\operatorname{dens}(x_j,K)}
\end{aligned}
\]
MEANS: $N_i$ = $K$ nearest neighbours of row $i$; density = inverse average neighbour distance; ARD = density relative to neighbours. READ: small density or small ARD is outlying; ARD is useful when clusters have different density.

\subsection*{ASSOCIATION RULES / APRIORI}
\[
\fid[eq:assoc]\begin{aligned}
\operatorname{supp}(X)&=\frac{\#(X)}N,\\
\operatorname{conf}(X\to Y)&=\frac{\operatorname{supp}(X\cup Y)}
{\operatorname{supp}(X)},\\
\operatorname{lift}(X\to Y)&=\frac{\operatorname{conf}(X\to Y)}
{\operatorname{supp}(Y)}
\end{aligned}
\]
MEANS: $X,Y$ = disjoint itemsets; $X$ = left side/antecedent; $Y$ = right side/consequent; $\#(X)$ = baskets containing all items in $X$; $N$ = baskets. CALC: count baskets with $X$, $Y$, and $X\cup Y$. READ: support = frequency; confidence = $P(Y|X)$; lift $>1$ means $Y$ is more common after seeing $X$ than baseline. APRIORI: if an itemset is below minimum support, all supersets are also below.
